package com.example.legendutils.Views;

import android.app.Dialog;
import android.content.Context;

public class OpenFileDialog extends Dialog {

    public OpenFileDialog(Context context) {
        super(context);
        // TODO 自动生成的构造函数存根
    }

    public OpenFileDialog(Context context, int theme) {
        super(context, theme);
        // TODO 自动生成的构造函数存根
    }

    public OpenFileDialog(Context context, boolean cancelable, OnCancelListener cancelListener) {
        super(context, cancelable, cancelListener);
        // TODO 自动生成的构造函数存根
    }

}
